name: Auth Service - Build & Push

on:
  push:
    branches:
      - initial_try

jobs:
  build-and-push:
    name: Build & Push Auth Images
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push auth-service
        uses: docker/build-push-action@v5
        with:
          context: ./auth-service
          file: ./auth-service/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/bookreview-auth:latest
            ${{ secrets.DOCKER_USERNAME }}/bookreview-auth:${{ github.sha }}

      - name: Build & Push auth-db
        uses: docker/build-push-action@v5
        with:
          context: ./auth-service
          file: ./auth-service/db.Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/bookreview-auth-db:latest
            ${{ secrets.DOCKER_USERNAME }}/bookreview-auth-db:${{ github.sha }}
